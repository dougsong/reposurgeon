= TO-DO =

* mergeinfo interpretation needs some work. Present implementation may
  create confusing merge links in some cases.

* The fossil map isn't partitioned on an expunge.

* repodiffer needs to compare tags and be able to emit diff reports.
